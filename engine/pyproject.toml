[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "ego-audit-engine"
version = "0.1.0"
description = "Motor de contabilidad (posicion/PnL) en Rust para Ego Audit"
requires-python = ">=3.10"

[tool.maturin]
module-name = "ego_audit_engine"
